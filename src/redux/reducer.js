// src\redux\reducer.js
// initial state
const initState = {};

export const rootReducer = (state = initState, action) => {
    switch (action.types) {
        default:
            return state;
    }
};
